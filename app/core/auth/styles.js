import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  bgImage: {
    flex: 1,
    height: 'auto',
    width: 'auto',
    alignItems: 'center',
  }
});

export default styles;